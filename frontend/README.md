# NUMA Frontend

React 19 + Tailwind CSS + Radix UI — plataforma educativa gamificada.

## Desarrollo

```bash
yarn install
yarn start
```

Corre en `http://localhost:3000`. Requiere el backend corriendo en `http://localhost:8000`.
